<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Shop Website</title>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <!-- Include custom CSS --> 
    <link rel="stylesheet" href="/css/blog.css">
    <link href="/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
     <!-- Custom styles for this template -->
     <link href="https://fonts.googleapis.com/css?family=Playfair&#43;Display:700,900&amp;display=swap" rel="stylesheet">
     <!-- Custom styles for this template -->
</head>
<body>
    <!-- Navbar -->
        <header class="blog-header py-3">
            <div class="row flex-nowrap justify-content-between align-items-center">
              <div class="col-4 pt-1">
                <a class="link-secondary" href="#">Home</a>
              </div>
              <div class="col-4 text-center">
                <a class="blog-header-logo text-dark" href="#">King</a>
              </div>
              <div class="col-4 d-flex justify-content-end align-items-center">
                <% if (loggedIn) { %>
                    <a class="btn btn-sm btn-danger" href="/logout">Logout</a>
                <% } %>
        
                <% if (!loggedIn) { %>
                    <a class="btn btn-sm btn-outline-secondary mx-3" href="/login">Sign In</a>
                    <a class="btn btn-sm btn-success" href="/register">Sign Up</a>
                <% } %>
              </div>
    </header>
    
    <div class="mt-15">
        <div class="maq-out">
            <div class="row m-0">
                <div class="col-lg-12">
                    <div class="maq-in">
                        <marquee class="marquee-text" behavior="scroll" direction="left" onmouseover="this.stop();" onmouseout="this.start();" style="vertical-align: middle;">ระบบเติมเงินเกมส์ออนไลน์สามารถใช้งานได้ปกติแล้วนะคะ ลูกค้าสามารถทำการสั่งซื้อได้ด้วยตนเองผ่านระบบอัตโนมัติได้ตลอด 24 ช.ม. ขอบคุณที่ใช้บริการ</marquee>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Section -->
    <section class="hero bg-primary text-white text-center py-5">
        <h1>Welcome to Our Shop</h1>
        <p>Discover amazing products and deals.</p>
    </section>
    <!-- Product Section --> 
    <section class="products py-5">
        <div class="container">
            <div class="section-title text-left mb-20 pt-20" style="padding-left: 1px; padding-right: 1px;">
              <h2>Streaming</h2>
              <div class="row">
                <!-- Product Category: Games -->
                <ul class="products-grid">
                    <% streamingData.forEach(product => { %>
                      <li class="product-item">
                        <div class="card">
                          <img src="<%= product.img %>" alt="<%= product.title %>">
                          <div class="card-body">
                            <h2><%= product.title %></h2>
                            <p><%= product.reviews %></p>
                            <p>Price: $<%= product.newPrice %></p>
                            <p><%= product.days %> Days</p>
                            <a href="/topup" class="btn btn-primary">buy now</a>
                          </div>
                        </div>
                      </li>
                    <% }); %>
                  </ul>
              </div>
            </div>
          </div>
    </section>
                <!-- Add more product cards here -->
                <section class="products py-5">
                    <div class="container" >
                        <div class="section-title text-left mb-20 pt-20" style="padding-left: 1px; padding-right: 1px;">
                            <h2>Games</h2>
                        <div class="row">
                            <!-- Product Category: Games -->
                            <ul class="products-grid">
                                <% gamesData.forEach(product => { %>
                                  <li class="product-item">
                                    <div class="card" style="margin-bottom:10px;">
                                      <img src="<%= product.img %>" alt="<%= product.title %>">
                                      <div class="card-body">
                                        <h2><%= product.title %></h2>
                                        <p><%= product.reviews %></p>
                                        <p>Price: $<%= product.newPrice %></p>
                                        <p><%= product.points %> Points</p>
                                        <a href="/topup" class="btn btn-primary">buy now</a>
                                      </div>
                                    </div>
                                  </li>
                                <% }); %>
                              </ul>
                          </div>
                        </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
</body>
</html>
